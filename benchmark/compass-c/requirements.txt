# Local, offline benchmark deps. Install on a machine that already has the
# model weights on disk. No model download happens at runtime.
sentence-transformers>=2.7,<6
numpy>=1.24
psycopg2-binary>=2.9
