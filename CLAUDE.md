# zachi.co.il — הנחיות עבודה

מונורפו של אתר טרום-ההשקה לספר „מדייטים לאהבה” (צחי חן). קוד האתר תחת `site/`
(Next.js App Router — ראו `site/AGENTS.md`: זו אינה גרסת ה-Next.js הסטנדרטית,
יש לקרוא את `node_modules/next/dist/docs/` לפני כתיבת קוד).

## Release — כללי מיזוג והשקה (מחייב)

הכללים האלה גוברים על כל הנחיה כללית אחרת, כולל „תמשיך”.

1. **אין מיזוג ל-`main` בלי אישור מפורש מצחי בהודעה נפרדת.**
   „תמשיך”, „continue”, „complete the release” או כל הנחיה קודמת — **אינם**
   אישור מיזוג. האישור חייב להיות הודעה חדשה ונפרדת שאומרת במפורש „מזג ל-main”
   (או שווה-ערך חד-משמעי) עבור אותו שינוי ספציפי.

2. **עובדים ב-feature branch בלבד.** כל שינוי נעשה בענף ייעודי, מקבל commit
   ו-push לענף, ומדווח — כולל סטטוס הבנייה של הענף. לאחר מכן **עוצרים ומחכים**
   לאישור מיזוג מפורש.

3. **אם אי-אפשר לקרוא את סטטוס הבנייה — לא ממזגים.** כשאין גישה לקרוא את סטטוס
   ה-build (למשל כלי GitHub מנותקים, או פרוקסי חוסם את Vercel/הדומיין), מדווחים
   על כך במפורש ו**עוצרים**. לא ממזגים ל-`main` „על עיוור”, גם אם הבנייה
   המקומית ירוקה.

4. **הפרדה בין פעולות.** push לענף feature, מחיקת ענף שכבר מוזג, ודיווח סטטוס —
   מותרים בלי אישור מיוחד. **מיזוג ל-`main` ופריסה לפרודקשן — תמיד דורשים את
   האישור הנפרד מסעיף 1.**

היסטוריה: בעבר בוצע מיזוג ל-`main` על סמך „תמשיך”. זה לא תקין. „תמשיך” מקדם
עבודה בענף — הוא לעולם אינו מאשר מיזוג.

## Pre-Launch — גבולות תוכן

- הספר עדיין **לא למכירה**. אין checkout / רכישה / „קנה עכשיו” / דחיפות שקרית.
- אין להמציא: המלצות קוראים, מספרי נרשמים, מיקום בתור, שמות פרקים, תוצאות או
  תוכן מהספר. סלוט ההמלצות נשאר ריק עד שיתקבלו המלצות אמיתיות ומאושרות.
- זהות עיצובית: Ink / Ivory / Sage / Terracotta (Terracotta = דגש מבוקר בלבד).
